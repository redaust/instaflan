<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top bg-light border-bottom border-gray">
  <div class="container-fluid w-70">
    <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag 'instaflan_logo.png', width: "100rem" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

<div class="d-flex px-2">
        <form class="d-flex" role="search">
          <input class="form-control me-2 border-secondary" type="search" placeholder="Search" aria-label="Search" >
          <button class="btn btn-outline-secondary" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
        </form>
      </div>
      <br/>
 
      <div class="navbar-collapse gap-3 w-70 px-2"> 
          <%= link_to root_path, class:"text-dark" do %><i class="fa-sharp fa-solid fa-house"> Home</i> <% end %>
          <i class="fa-brands fa-facebook-messenger">Messenger</i>
          <%= link_to new_post_path, class:"text-dark" do %><i class="fa-solid fa-square-plus">Add Post</i> <% end %>
          <i class="fa-solid fa-compass">Discover</i>
          <i class="fa-solid fa-heart">Likes</i>
        </div>
               <% if current_user && current_user.profile_pic.attached? %>
     <%= image_tag current_user.profile_pic, class: "img-thumbnail rounded", style: "width: 5%" %>
     <% else %>
     <%= image_tag 'flanatar.png', class: "img-thumbnail rounded", style: "width: 5%" %>
     <% end %>
   </div>
    </div>
  </div>
    </div>
  </div>
</nav>